# Reference

Long-form context that shouldn't load every session. One topic per file,
named plainly (e.g. `infra.md`, `product-spec.md`, `writing-samples.md`).

Claude: this directory is NOT auto-loaded. List it and read the relevant
file when a task touches one of these topics.
